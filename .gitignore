__pycache__/
*.pyc
*.egg-info/
/dist/
/build/
.syncrun/
tmp/
log/
logs/
settings.json
settings_*.json
!example_settings.json


# pytest 產生的暫存檔
.pytest_cache/

# 測試覆蓋率報告與相關檔案
.coverage
.coverage.*
coverage.xml
htmlcov/

# 測試產生的日誌 (視你的設定而定)
*.log
config
*.whl
.sftp_upload_manifest.json
.sftp_download_manifest.json
fleet_logs/

# 版本標記:上傳前由 version_stamp.py 產生的「產物」,不進 git(對外版號的宣告是
# VERSION.json,那個要人工編輯、要進 git)。它會隨鏡像上船,這正是預期行為。
/VERSION.stamp.json

# AI 工具的本機工作目錄。.claude/ 底下會有 worktrees/(整份 repo 副本),
# 一旦沒忽略就會以 "?? .claude/" 出現在 git status —— 在待傳輸專案裡,
# 那等同讓 version_stamp.py 把該次發布記成 dirty: true。
.claude/
.agents/
.codex/

# 部署用的密碼檔(deploy_offline.sh)。正式位置是 config/deploy_sudo_pass.txt 與
# config/deploy_remote_pass.txt,而 config/ 上面那條就已經 ignore 掉整個目錄了
# (注意:ignored 不等於不散佈 —— config/ 會經 OTA 傳到全船隊)。
# 下面兩條只是保險:有人把密碼檔放到 repo 根目錄時仍然不會被 commit。
.nssms_deploy_pass
.nssms_remote_pass
